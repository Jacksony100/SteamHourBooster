# Repo Hygiene

## State found
- The whole 2.0 rewrite was **untracked** (only `main.py` committed). Fixed by a
  safety-baseline commit on `fix/beta-close-all-design-steam-assets`.
- Build/DB artifacts present in the working tree but **not** tracked in git:
  `apps/web/.next/`, `apps/web/tsconfig.tsbuildinfo`, `test.db`, four
  `DeckPilot-*.zip` snapshot archives. None were in git history, so no
  `git rm --cached` was required.

## .gitignore additions
```
*.zip                      # project snapshot archives (local backups)
.design-import/            # extracted design archive working dir
apps/web/.next/            # belt-and-suspenders (already covered by .next/)
apps/web/tsconfig.tsbuildinfo
```
Already ignored previously: `__pycache__/`, `.pytest_cache/`, `.next/`, `*.db`,
`*.sqlite3`, `.env`, `key.key`, `node_modules/`, `.vercel/`, `coverage/`.

## Recommended local cleanup (safe — these are not tracked)
```bash
rm -f DeckPilot-*.zip          # local snapshot backups, if no longer needed
rm -f test.db                  # regenerated by tests
rm -rf apps/web/.next          # regenerated by `npm run build`
rm -rf .design-import          # re-extract from the design archive if needed
```

## Verified
`git diff --cached` on the baseline commit contains **no** `.next/`, `test.db`,
`.zip`, `node_modules/`, or `__pycache__` paths.
